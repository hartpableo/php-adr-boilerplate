{% extends 'base.html.twig' %}

{% block title %}User List{% endblock %}

{% block body %}
    <h1>User List</h1>
    <ul>
      {% for user in users %}
        <li>{{ user.name }} ({{ user.email }})</li>
      {% endfor %}
    </ul>
    <p><a href="/signup">Sign up new user</a></p>
{% endblock %}